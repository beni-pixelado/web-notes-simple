version 0.2 log

since the last version a sidebar was added and in which has the functionality of
changing the color of texts marked as content

all pages were optimized for much less lag

optimized interface and persistent databases were also added

the function to add images was also inserted

among other things